{
  "version": "1.27.0",
  "type": "coc-use-template",
  "description": "COC USE template for Ruby SDK projects (Magnus bindings) (regenerated by /sync from loom@0266594 + 2.31.0)",
  "released": "2026-05-18",
  "upstream": {
    "name": "loom",
    "type": "coc-source",
    "version": "2.31.0",
    "synced_at": "2026-05-18T11:29:38Z",
    "loom_sha": "0266594",
    "template_version": "2.31.0",
    "sdk_packages": {
      "workspace": "4.0.0",
      "independent_crates": {
        "eatp": "0.1.0",
        "kailash-governance": "0.1.0",
        "kailash-pact": "0.1.0"
      }
    }
  },
  "changelog": [
    {
      "date": "2026-05-18",
      "version": "1.27.0",
      "summary": "FINAL DRIFT-CLOSE LEG — brings rb from loom@a9d743f to loom@0266594 (PR #286 merged: chore/self-referential-codify-tier-demotion). Closes the 5-template drift surfaced at final cross-verification: rb was the lone straggler carrying the PRE-DEMOTION self-referential-codify.md (`priority:0 / scope:baseline`) with NO codify.md self-referential gate directive, while the other 4 USE templates (kailash-coc-claude-py, kailash-coc-py, kailash-coc-claude-rs, kailash-coc-rs) were all at 0266594 with the path-scoped rule + directive. ROOT CAUSE: PR #35 (rb follow-up, a217488) delivered self-referential-codify.md but ran BEFORE the PR #286 demotion existed — it shipped the baseline-scoped version. loom main subsequently advanced a9d743f→0266594 (PR #286, b4993d3): (1) self-referential-codify.md frontmatter demoted `priority:0/scope:baseline` → `priority:10/scope:path-scoped` + 7-glob `paths:` (.claude/{commands,rules,skills,hooks,bin}/**, agents/management/**, audit-fixtures/violation-patterns/**), (2) new Rule-2 'paths: frontmatter is the load-trigger SUPERSET; this allowlist is the firing-scope SUBSET' note, (3) line-18 markdown-render artifact (L5_DELEGATED→L5*DELEGATED, em-dash escaping) — loom HEAD canonical state, distributed verbatim per artifact-flow.md (loom is the splitter; /sync ships loom HEAD as-is, does NOT independently re-fix source — the other 4 templates carry the identical 0266594 content), (4) codify.md §6 gained the 'Self-referential gate (MANDATORY, posture-independent)' directive instructing the orchestrator to evaluate self-referential-codify.md Rule-2's allowlist from the always-loaded command body BEFORE convergence (structural guarantee since the now-path-scoped rule may not be in context at framing time). NET RB-RELEVANT DELTA = EXACTLY 2 FILES: rules/self-referential-codify.md + commands/codify.md. sync-manifest.yaml (3rd changed .claude/ file in a9d743f..0266594) is NOT synced to cc-only-legacy rb (manifest is multi-CLI-only / exclude). journal/0101-0102 (2 added files) NOT synced (loom-internal). Both delivered files are GLOBAL-ONLY (no variants/rb/{rules,commands}/ overlay exists — verified absent) → straight copy of loom committed HEAD 0266594, NO compose.mjs (grep '^<!-- slot:' = 0), NO Step-2.5 overlay-repair needed for these 2 (this leg touched no rb-overlaid path; the 26 rb full-file overlays were untouched and verify-overlays.sh rb stayed 26/26 pre AND post). Byte-identity confirmed vs loom HEAD 0266594 AND vs the kailash-coc-claude-py canonical template (already at 0266594) for both files. No loom version bump in a9d743f..0266594 (git diff .claude/VERSION empty — PR #286 is a chore/tier-demotion, not a release) → upstream.version stays 2.31.0; ONLY provenance corrected: upstream.loom_sha a9d743f → 0266594. template_version (rb-local top-level) 1.26.0 → 1.27.0 for this 2-file content delta; upstream.template_version stays 2.31.0 (canonical: == upstream.version == loom version per sync-completeness.md MUST-3). Success criterion: rb at 0266594 matching the other 4 templates' rule scope + directive — MET.",
      "changes": [
        "RULE REPLACED (drift-close): rules/self-referential-codify.md — rb's PRE-DEMOTION copy (`priority:0 / scope:baseline`, NO paths:) REPLACED with the canonical loom@0266594 version (`priority:10 / scope:path-scoped` + 7-glob paths: + new Rule-2 glob-superset note + line-18 markdown-render-artifact state). Byte-identical to loom HEAD 0266594 AND to kailash-coc-claude-py canonical (diff = empty for both). This is the core drift-close: rb's rule scope now matches the other 4 templates. NO rb overlay (variants/rb/rules/self-referential-codify.md verified absent) → global emitted as-is per artifact-flow.md Variant Overlay Semantics 'Global only'.",
        "COMMAND REPLACED (drift-close): commands/codify.md — §6 'Red team the agents and skills' gained the 'Self-referential gate (MANDATORY, posture-independent)' directive (grep count 0 PRE → 1 POST). The directive instructs the /codify orchestrator to match every proposal-touched file against self-referential-codify.md Rule-2's allowlist from the always-loaded command body and run the multi-agent redteam-with-tests round regardless of trust posture — the structural guarantee that survives the rule's path-scoped demotion (rule may not be in context at framing time). Byte-identical to loom HEAD 0266594 AND to kailash-coc-claude-py canonical. NO rb overlay (variants/rb/commands/codify.md verified absent; rb command overlays are ai/api/cc-audit/db/release/sdk/sync/test only) → global emitted as-is.",
        "DELTA SCOPE: git diff --name-status a9d743f..0266594 = 3 .claude/ files (codify.md M, self-referential-codify.md M, sync-manifest.yaml M) + 2 journal/ adds (0101, 0102). sync-manifest.yaml NOT synced to rb (cc-only-legacy; manifest is multi-CLI-only per repos.rb exclusion). journal/* NOT synced (loom-internal). Net rb-relevant changed-file count = EXACTLY 2 (the rule + codify.md). No other rb-tier-subscribed ([cc,co,coc]) file changed in range.",
        "OVERLAY PRESERVATION: tools/verify-overlays.sh rb = Passing 26 / Failing 0 (PRE and POST — unchanged). This leg touched only 2 GLOBAL-ONLY files with NO rb overlay; zero rb full-file overlay paths were written → NO driver raw-copy regression occurred → NO Step-2.5 repair needed this leg (unlike PR #34/#35 driver-based legs where the canonical sync-tier-aware.mjs raw-copies loom globals over the 26 overlays and Step-2.5 repair re-applies them). All 26 rb legacy-full-file overlays (8 commands + 13 rules + 5 skill SKILL.md) byte-unchanged.",
        "OBSOLETED PURGE: no-op. 0 of 41 union(obsoleted=28 + use_obsoleted=13) paths present in rb (clean since PR #34/1.23.0). .coc-obsoleted unchanged (44 lines = 41 entries + 3 header comment lines). No obsoleted-list change in a9d743f..0266594.",
        "STEP 7.5 ALLOWLIST: self-referential-codify.md + codify.md contain 0 src/kailash/ references → .github/coc-sdk-refs-allowlist.txt extension = idempotent (0 new entries; comm -23 found∖allowed = empty). No append.",
        "VERSION SCHEMA (sync-completeness.md MUST-3): canonical schema preserved. upstream.{name,type,version,synced_at,loom_sha,template_version,sdk_packages} all present. upstream.version=2.31.0 (== loom HEAD .claude/VERSION at 0266594; git diff a9d743f..0266594 -- .claude/VERSION empty → loom version unchanged; PR #286 is chore not release). upstream.loom_sha a9d743f → 0266594 (provenance correction — the entire point of this leg's VERSION change). upstream.synced_at → 2026-05-18T11:29:38Z (real UTC). upstream.template_version stays 2.31.0. top-level version 1.26.0 → 1.27.0 (rb-local template version, +1 patch for 2-file content delta). sdk_packages carried forward unchanged (kailash-rs BUILD not in workspace; no source of truth this cycle).",
        "STRUCTURAL ASSERTIONS (coc-sync.md Step 8): settings.json hooks all resolve to .claude/hooks/*.js (count verified post-write); 0 scripts/hooks/ refs in settings.json; no .claude/scripts/ orphan dir; union(obsoleted+use_obsoleted) absent (0 present, 0 purged); 2 delivered files have ZERO BUILD-internal path leaks (no src/kailash/, /Users/, workspaces/, crates/, packages/).",
        "SDK PINS: NONE. loom sync-manifest.yaml carries no sdk_packages map; kailash-rs BUILD repo not in workspace this cycle; no pyproject/Cargo SDK pins for this Ruby template. Prior VERSION.upstream.sdk_packages block preserved unchanged (carry-forward; no source of truth).",
        "PRESERVED: CLAUDE.md (template-owned, never overwritten per coc-sync.md Step 7); .claude/learning/** + .proposals/** + settings.local.json (never synced per sync-manifest.yaml::exclude). .coc-sync-marker refreshed (this leg's marker).",
        "CC-ONLY: rb is clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission (.codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md intentionally absent per repos.rb.templates[0].clis=[claude]). NO emit.mjs --lang headroom step — sync-completeness.md v6.2 headroom-floor BLOCK does not apply to cc-only-legacy rb (no Codex/Gemini baseline = no emission-fit dimension; the emission-fit reason for the PR #286 demotion does not apply to rb, but cross-template rule-scope consistency does — rb now carries the canonical path-scoped rule + codify.md directive like every other template). headroom_pct n/a for rb verification table.",
        "DISCLOSURE: node .claude/bin/scan-synced-disclosure.mjs --check exit 0 (PASS, no BLOCK) — run FIRST per coc-sync.md Step 1 / artifact-flow.md Gate-2 output fence."
      ]
    },
    {
      "date": "2026-05-18",
      "version": "1.26.0",
      "summary": "FOLLOW-UP LEG to PR #34 (mergeCommit be1ab38) — delivers rules/self-referential-codify.md, the ONE file PR #34 shipped WITHOUT. Root cause: PR #285 (F4) codified the rule at loom but its /codify did NOT add a companion sync-manifest.yaml tier entry (journal-0078 manifest-completeness failure class). The canonical rb driver (sync-tier-aware.mjs --target rb, cc-only-legacy) classified the untiered rule skip/no_tier_match (non-fatal, unlike py/rs's emit.mjs validator-15 hard-BLOCK), so rb committed (92121ab) + admin-merged PR #34 to main WITHOUT the rule but HONESTLY flagged the gap (PR #34 marker flagged: block). NOW FIXED at source: rules/self-referential-codify.md added to sync-manifest.yaml::tiers.co + per_rule_size_budget_bytes:14000 via Co-Owner-Directed Origination (artifact-flow.md § Co-Owner-Directed Origination; receipt-before-edit journal/0101, verbatim co-owner directive 'approved'). rb subscribes to [cc,co,coc] → co-tier now reaches rb. Re-ran canonical sync-tier-aware.mjs --target rb: copied 922 (was 921 in PR #34, +1 = self-referential-codify.md), no_tier_match 35 (was 36, -1 = rule moved out of no_tier_match into tier-matched), purged 0. Step-2.5 overlay repair re-applied all 26 rb legacy full-file overlays as-is post-driver (driver raw-copies loom globals which would regress them). verify-overlays.sh rb 26/26 PASS pre + post. PR #34's cumulative content (PR #284 redteam-integration.md 170 lines etc.) is LEGITIMATE and NOT reverted — only the missing rule is owed. Net rb working-tree delta vs PR #34: exactly +1 file (the rule). upstream.version/template_version stay 2.31.0 (loom HEAD a9d743f unchanged, no release commit; PR #285 + journal/0101 are post-2.31.0 codify/chore). template_version (rb-local) 1.25.0 → 1.26.0 for this content delta.",
      "changes": [
        "RULE NEW (NOW LANDED — gap closed): rules/self-referential-codify.md (PR #285 f3a5e3b, F4) — 13321 B, IDENTICAL to loom HEAD global (no rb overlay; global emitted as-is). This is the entire purpose of the follow-up leg. PR #34 marker no_tier_match:36 explicitly listed this rule as FLAGGED; the gap is now closed.",
        "SOURCE-SIDE FIX (Co-Owner-Directed Origination, receipt-before-edit journal/0101): sync-manifest.yaml gained (a) `- rules/self-referential-codify.md` in tiers.co (after rules/facade-manager-detection.md), (b) `\"self-referential-codify.md\": 14000` in cli_variants...per_rule_size_budget_bytes. All three artifact-flow.md conditions verified met by the receipt: verbatim directive ('approved'), receipt-before-edit (journal/0101 staged before edit), COC-tooling scope (sync-manifest.yaml tier membership).",
        "DRIVER RE-RUN: node .claude/bin/sync-tier-aware.mjs --target rb (same canonical driver PR #34 used, PR #275/#272 8a92864). Reads sync-manifest.yaml from working tree (fs.readFileSync) → correctly picks up the in-flight co-tier addition. Single-session orchestration-root run, no parallel editor (working tree = manifest edit + observations.jsonl + journal/0101 receipt + worktrees/ only); agents.md 'Concurrent Readers Read Committed HEAD' guards against PARALLEL mid-edit WIP — N/A here (the edit IS this session's deliberate source-side fix). copied 922 / purged 0 / skipped exclude=770 use_exclude=31 no_tier_match=35.",
        "OVERLAY PRESERVATION (Step-2.5 repair): 26/26 rb full-file overlays (8 commands + 13 rules + 5 skill SKILL.md) re-applied as-is post-driver via portable cp (BSD install -D unsupported on macOS; corrected). All 26 are legacy-full-file form (grep '^<!-- slot:' = 0). verify-overlays.sh rb: Passing 26 / Failing 0 PRE-driver, transiently 0/26 after driver raw-copy, 26/0 restored POST-repair. Writing generic global over these would be a regression — explicitly repaired.",
        "FILE-COUNT DELTA vs PR #34: exactly +1 file (rules/self-referential-codify.md). All other co-tier globals already current from PR #34 (which synced full a9d743f range); the 26 overlay raw-copies were restored to byte-identical PR #34 state. Net rb git status = 1 untracked file.",
        "OBSOLETED PURGE: no-op. All obsoleted: + use_obsoleted: paths already absent in rb (clean since 1.23.0/PR #34). .coc-obsoleted unchanged (44 entries). No obsoleted-list change this cycle.",
        "STEP 7.5 ALLOWLIST: self-referential-codify.md has 0 src/kailash/ references → no .github/coc-sdk-refs-allowlist.txt extension needed. Idempotent (0 new entries).",
        "STRUCTURAL ASSERTIONS (coc-sync.md Step 8): 23/23 settings.json hooks resolve to .claude/hooks/*.js; 0 scripts/hooks/ refs in settings.json; no .claude/scripts/ orphan dir; union(obsoleted+use_obsoleted) absent (0 purged); new rule has ZERO BUILD-internal path leaks (no src/kailash/, /Users/, workspaces/, crates/).",
        "SDK PINS: NONE. loom sync-manifest.yaml carries no sdk_packages map; kailash-rs BUILD repo not in workspace this cycle. Prior VERSION.upstream.sdk_packages block preserved unchanged (carry-forward; no source of truth this cycle).",
        "PRESERVED: CLAUDE.md (template-owned, never overwritten per coc-sync.md Step 7); .claude/learning/** + .proposals/** + settings.local.json + .coc-sync-marker (never synced per sync-manifest.yaml::exclude).",
        "CC-ONLY: rb is clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission. NO emit.mjs --lang headroom step (sync-completeness.md v6.2 headroom-floor BLOCK does not apply to cc-only-legacy rb). headroom_pct n/a for rb verification table."
      ]
    },
    {
      "date": "2026-05-18",
      "version": "1.25.0",
      "summary": "loom@951eaea → a9d743f HEAD-advance sync (rb CC-only; NO loom version bump — upstream.version/template_version stay 2.31.0 per sync-completeness.md MUST-3; PRs #278-#285 are post-2.31.0 codify/chore commits, no release commit in range). Distribute-only cycle (Gate 1 SKIPPED — proposal status=distributed; PRs #284/#285 loom-originated, journal/0099+0100 receipts). Range adds: F4 self-referential-codify rule (PR #285) + 3 redteam-arc patterns in redteam-integration.md (PR #284) + forest-ledger /wrapup Option-B redesign (PR #283: persistent outstanding-ledger + validate-forest-ledger.mjs + 27 forest-ledger audit fixtures) + canonical sync-tier-aware.mjs (PR #275/#272) + codify Step-7b self-contained schema (PR #281) + artifact-flow Co-Owner-Directed-Origination clause + trust-posture self_referential_codify_without_redteam emergency trigger. Sync executed via canonical .claude/bin/sync-tier-aware.mjs --target rb (tier-aware [cc,co,coc] enumeration, NOT ad-hoc bulk copy per coc-sync.md MUST-NOT). verify-overlays.sh rb 26/26 pre + post.",
      "changes": [
        "TOOLING: distribution executed via canonical `node .claude/bin/sync-tier-aware.mjs --target rb` (landed THIS range, PR #275/#272 8a92864). Tier-subscription-filtered emission ([cc,co,coc] union); 921 tier-matched copied, 770 exclude, 31 use_exclude, 36 no_tier_match, 0 purged (use_obsoleted paths already absent). Ad-hoc bulk `cp -r .claude/` BLOCKED per coc-sync.md MUST-NOT (2026-05-09 leak-path precedent).",
        "RULE NEW (NOT LANDED — manifest classification gap, FLAGGED): rules/self-referential-codify.md (PR #285 f3a5e3b, F4). Codified at loom but NOT a member of any sync-manifest.yaml tier (sync-manifest.yaml unmodified in 951eaea..a9d743f). Classifies skip/no_tier_match on rb AND rs AND py — the new rule reaches NO USE template. Correct conservative Gate-2 behavior: NOT auto-placed (artifact-flow.md 'Human Classifies Every Change' + 'loom Splits, Never Originates'; tier assignment is a Gate-1 human act, and Gate 1 was explicitly SKIPPED this distribute-only cycle). REQUIRES loom Gate-1 tier classification next /sync cycle.",
        "RULE UPDATE: rules/trust-posture.md — MUST Rule 4 emergency-trigger list gains `self_referential_codify_without_redteam` (1× = drop 1 posture), the teeth-side companion to the new (untiered) self-referential-codify.md. Global; rb has no trust-posture overlay → emitted as-is. This 1-line ref DID land (trust-posture.md IS tiered) even though the rule it references did not — expected asymmetry, surfaced for audit.",
        "RULE UPDATE: rules/artifact-flow.md — new § 'Co-Owner-Directed Origination (narrow, receipt-gated exception)' + 'Repo Classes Map 1:1 To Resolver Logical Keys' (PR #283 chain). Global, no rb overlay → emitted as-is.",
        "COMMAND UPDATE: commands/codify.md — self-contained Step 7b schema for USE-template origination (PR #281 5b03abf). NOT an rb overlay path (rb command overlays: ai/api/cc-audit/db/release/sdk/sync/test only) → global emitted as-is.",
        "COMMAND UPDATE: commands/wrapup.md — forest-ledger Option-B redesign: persistent outstanding-ledger + ID-contract (PRs #283 8afd84f..ad27458, R2-R7 redteam convergence). NOT an rb overlay path → global emitted as-is.",
        "SKILL UPDATE: skills/30-claude-code-patterns/sync-flow.md (cc-tier) + skills/32-trust-posture/redteam-integration.md (coc-tier) — 3 forest-ledger-arc redteam patterns (PR #284 79646e0). Global, no rb overlays → emitted as-is.",
        "BIN NEW: bin/sync-tier-aware.mjs (canonical tier-aware sync driver, PR #275 8a92864) + bin/validate-forest-ledger.mjs (PR #283 8afd84f, forest-ledger ID-contract validator). Tier-independent global infra (.claude/bin/** always_include). Absent in rb pre-sync — genuine new infra.",
        "BIN UPDATE: bin/sync-consumer-dryrun.mjs (PR #283-range, 11591→11597 bytes). Tier-independent global infra.",
        "FIXTURE NEW: 27 audit-fixtures/forest-ledger/* (fixture-01..27 .exit/.expected/.session-notes + run.mjs, PR #283). Tier-matched (cc/co/coc). Mandatory companion fixtures for validate-forest-ledger.mjs per cc-artifacts.md Rule 9.",
        "OVERLAY PRESERVATION: 26/26 rb full-file overlays (13 rules + 8 commands + 5 skill SKILL.md) — ZERO changed in 951eaea..a9d743f (verified `git diff --stat 951eaea..a9d743f -- .claude/variants/rb/` empty). All are legacy-full-file form (grep '^<!-- slot:' = 0 across all 26). Canonical driver raw-copies loom globals (would regress these 26); Step-2.5 repair re-applied all 26 overlay files as-is post-driver. verify-overlays.sh rb: 26/26 PASS pre AND post (variant-applied(rb,full)). Writing generic global over these would be a regression — explicitly repaired.",
        "STEP 7.5 ALLOWLIST: .github/coc-sdk-refs-allowlist.txt auto-extended (append-only, date-stamped 2026-05-18/v2.31.0 block) with 2 new entries (audit-fixtures/scan-synced-disclosure/r3-smuggle-closed/.claude/rules/cleanlocks.md + bin/scan-synced-disclosure.mjs). Idempotency re-verified post-append (comm -23 found∖allowed = empty). Stale-but-benign prior entries (07-sync-flow.md, sdk-upstream-donation.md — now use_excluded) NOT auto-removed per Step 7.5 MUST NOT (removal is a human decision).",
        "STRUCTURAL ASSERTIONS (coc-sync.md Step 8): 17/17 settings.json hooks resolve to .claude/hooks/*.js; 0 scripts/hooks/ refs in settings.json; no .claude/scripts/ orphan dir; union(obsoleted+use_obsoleted) absent (0 purged — already clean from 1.23.0 cycle); .coc-obsoleted regenerated (44 entries, unchanged). self-referential-codify.md correctly ABSENT in rb (no tier).",
        "SDK PINS: NONE. loom sync-manifest.yaml carries no sdk_packages map; kailash-rs BUILD repo not present in workspace this cycle. No pyproject/Cargo SDK pins exist for this Ruby template. Prior VERSION.upstream.sdk_packages block preserved unchanged (carry-forward per task instruction; no source of truth available this cycle).",
        "PRESERVED: CLAUDE.md (template-owned, never overwritten per coc-sync.md Step 7); .claude/learning/** + .proposals/** + settings.local.json + .coc-sync-marker (never synced per sync-manifest.yaml::exclude).",
        "SHARED-CHECKOUT DISCIPLINE: loom HEAD a9d743f; working tree clean at session start (only M observations.jsonl + ?? worktrees/, both excluded). Canonical driver reads from REPO/.claude/ committed-state checkout; no parallel editor active.",
        "CC-ONLY: rb is clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission (.codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md intentionally absent per repos.rb.templates[0].clis). NO emit.mjs --lang headroom step (no codex/gemini baseline — sync-completeness.md v6.2 headroom-floor BLOCK does not apply to cc-only-legacy rb). headroom_pct n/a for rb verification table."
      ]
    },
    {
      "date": "2026-05-16",
      "version": "1.23.0",
      "summary": "loom@6bd4075 FULL catch-up sync (rb CC-only): resolves the explicitly-deferred 53dd297-narrow-cycle drift PLUS PRs #245/#246/#247. The prior 2026-05-16T11:13Z cycle (loom@53dd297) was deliberately NARROW (2 files; its own marker stated 'Unrelated prior-cycle version-lag drift left as-is / NOT actioned'). This cycle brings rb fully current to loom HEAD across the [cc,co,coc] subscribed tiers. PR #245: manifest tier reconciliation + Validator 15 (manifest-completeness) in emit.mjs. PR #246: RRPS standalone-consumer model + canonical-divergence gate added to sync-flow.md Downstream Sync (step 5a) + manifest YAML repair (RRPS consumer_overlays is a Python-downstream entry; N/A to the rb USE template). PR #247: Validator 16 (strict-YAML gate) in emit.mjs; cross-repo journal-receipt allowance in violation-patterns.js detectRepoScopeDriftBash + repo-scope-discipline.md User-Authorized Exception condition-4 marker; agents.md new MUST 'Worktree-Isolate Parallel Agents That Edit Shared Source' + Example 10. 14 files written (9 modified + 5 new). No loom version bump in range (PRs #245-247 are post-2.31.0 fixes, no release commit) — upstream.version/template_version stay 2.31.0; loom_sha 53dd297 → 6bd4075. SDK PINS: NONE — loom sync-manifest.yaml carries no sdk_packages map; kailash-rs BUILD repo not present in workspace; no pyproject/Cargo SDK pins to update for this Ruby template (prior VERSION sdk_packages preserved as-is, not re-read). G5 .gitignore defect N/A for rb (verified — rb ships a Ruby-gem .gitignore with no stock lib/ rule; git check-ignore confirms .claude/bin/lib/*.mjs not ignored; no precedent edit needed unlike py ad27b0f / rs c4615b9). No variants/rb/rules/agents.md full-copy exists — rb composes agents.md from the global neutral-body which already carries the new MUST (journal-0080-class flag N/A for rb). verify-overlays.sh rb 26/26 passing pre + post.",
      "changes": [
        "DEFERRED-DRIFT RESOLVED: the 53dd297 NARROW cycle explicitly left version-lag drift unactioned; this FULL catch-up resolves it. rb freeze baseline for genuine-lag computation = last full recompute loom@cde61d3/2.31.0.",
        "RULE UPDATE: rules/agents.md + guides/rule-extracts/agents.md — new MUST 'Worktree-Isolate Parallel Agents That Edit Shared Source; Concurrent Readers Read Committed HEAD' + Example 10 (PR #247, 9a5d76d). rb had 0 'Worktree-Isolate' matches pre-sync (genuine lag). No rb overlay for agents.md → global emitted as-is.",
        "RULE UPDATE: rules/repo-scope-discipline.md + guides/rule-extracts/repo-scope-discipline.md — cross-repo journal-receipt allowance / User-Authorized Exception condition-4 marker (PR #247, 9a5d76d). rb held older 53dd297 phrasing; refined version applied. No rb overlay → global as-is.",
        "RULE UPDATE: rules/sync-completeness.md + guides/rule-extracts/sync-completeness.md — v6.2 cycle-3 stale --strict-headroom doc-citation trim (19929f6 + cff3b30). rb was 205 lines (pre-trim); loom HEAD 182 lines. No rb overlay → global as-is.",
        "NEW RULE-EXTRACT: guides/rule-extracts/ci-runners.md — added at 6f600d5 (2.31.0 release). Claimed by rb 1.22.0 changelog but physically absent (this IS the deferred 53dd297-cycle drift). Companion to coc-tier rules/ci-runners.md (present in rb).",
        "NEW RULE-EXTRACT: guides/rule-extracts/orphan-detection.md — same as ci-runners.md: claimed-but-absent, now landed. Companion to coc-tier rules/orphan-detection.md.",
        "BIN UPDATE: bin/emit.mjs — Validator 15 (manifest-completeness, PR #245 1856627) + Validator 16 (strict-YAML gate, PR #247 9a5d76d) + parseArgs unknown-flag warn (b892aa7) + v6.2 cycle-2/3 strict-headroom default-flip. rb had none of these markers pre-sync (genuine lag). Tier-independent global infra (bin/**).",
        "BIN NEW: bin/sync-consumer-dryrun.mjs — new RRPS dry-run binary (PR #246). Tier-independent global infra (bin/**). Absent in rb pre-sync.",
        "HOOK UPDATE: hooks/lib/violation-patterns.js — cross-repo journal-receipt allowance added to detectRepoScopeDriftBash (PR #247 9a5d76d). coc-tier (hooks/lib/**).",
        "FIXTURE NEW: audit-fixtures/violation-patterns/detectRepoScopeDriftBash/authorization-receipt/{README.md,test.mjs} — mandatory companion fixtures for the violation-patterns.js change per cc-artifacts.md Rule 9. rb already carried 14 sibling fixtures for this detector; the 2 authorization-receipt files were absent.",
        "GUIDE UPDATE: skills/30-claude-code-patterns/sync-flow.md — RRPS standalone-consumer model + canonical-divergence gate step 5a added to Downstream Sync (PR #246 6417d3b). cc-tier (skills/30-claude-code-patterns/**).",
        "NO-OP (overlay-preservation): 26/26 rb full-file overlays (13 rules + 8 commands + 5 skill SKILL.md) — NONE changed in cde61d3..6bd4075; template correctly holds rb-composed output; verify-overlays.sh rb 26/26. Writing generic global over these would be a regression — explicitly NOT done.",
        "NO-OP (excluded): agents/management/coc-sync.md (exclude: loom-only); guides/co-setup/05-variant-architecture.md (use_exclude + use_obsoleted); sync-manifest.yaml/VERSION/variants-** (exclude). Correctly absent.",
        "SDK PINS: NONE. loom sync-manifest.yaml carries no sdk_packages map (verified <ABSENT>); kailash-rs BUILD repo not present in workspace. No pyproject/Cargo SDK pins exist for this Ruby template to update. Prior VERSION.upstream.sdk_packages block preserved unchanged (not re-read; no source of truth available this cycle).",
        "G5 .gitignore: N/A for rb — VERIFIED. rb ships a Ruby-gem .gitignore (only /lib/bundler/man/, no stock lib/ rule). git check-ignore -v .claude/bin/lib/slot-parser.mjs → NOT ignored. All 3 .claude/bin/lib/*.mjs already git-tracked + byte-identical to loom HEAD. No precedent .gitignore edit needed (unlike kailash-coc-claude-py ad27b0f / kailash-coc-claude-rs c4615b9).",
        "variants/rb/rules/agents.md full-copy: DOES NOT EXIST — rb composes agents.md from global neutral-body which already carries the new MUST. journal-0080-class 'variant full-copy missing G3 clause' concern N/A for rb (py/rs-specific).",
        "STRUCTURAL ASSERTIONS (coc-sync.md Step 8): zero scripts/hooks/ refs in settings.json; zero obsoleted paths present in rb (union of obsoleted + use_obsoleted verified absent); .coc-obsoleted regenerated (44 entries, unchanged vs prior). Step 7.5 allowlist idempotent (0 new entries; 15 allowlisted covers all 11 current src/kailash/ refs).",
        "PRESERVED: CLAUDE.md (template-owned, never overwritten per coc-sync.md Step 7); .claude/learning/** + .proposals/** + settings.local.json (never synced per sync-manifest.yaml::exclude).",
        "SHARED-CHECKOUT DISCIPLINE: all 14 loom source files read via `git show HEAD:<path>` (committed HEAD), not the working tree, per agents.md MUST 'Concurrent Readers Read Committed HEAD'. loom working tree left byte-exact."
      ]
    },
    {
      "date": "2026-05-15",
      "version": "1.22.0",
      "summary": "loom 2.30.0 → 2.31.0 sync (rb CC-only): v6.2 per-lang headroom validator landing (Shards 1+2+3) + F5 Trust Posture Wiring on sync-completeness.md + 2 new global rule-extracts upstreamed from kailash-py. Shard 1 adds `validateAggregateHeadroom()` to emit.mjs with per-CLI `headroom_floor_pct` declared in sync-manifest.yaml::cli_variants.context/root.md.<cli>; Shard 2 wires `--strict-headroom` into coc-sync.md's baseline-emission step so /sync invocations propagate non-zero exit on breach; Shard 3 adds the `headroom_pct` column to sync-completeness.md Rule 2's verification table. F5 binds that structural defense to the Trust Posture system (regression-within-grace classes covered, two-tier near-breach receipt band, manifest-axis + exit-code-swallow sweeps at /codify). The 2 rule-extracts (ci-runners.md, orphan-detection.md) hold post-mortem evidence and full code examples that exceed the 200-line rule budget for their parent rules. CC-only template — Codex/Gemini emitter outputs not consumed; headroom_pct n/a for rb baseline emission. SDK PINS: kailash-rs workspace 3.29.0 → 4.0.0 (major bump). Pre-flight + post-apply verify-overlays.sh rb 26/26 passing.",
      "changes": [
        "RULE UPDATE: rules/sync-completeness.md — F5 Trust Posture Wiring landed (v6.2 headroom-floor BLOCK condition wired into trust-posture.md grace + regression classes). Both rule + rule-extract refreshed.",
        "BIN UPDATE: bin/emit.mjs — validateAggregateHeadroom() + `--strict-headroom` flag for per-lang aggregate headroom enforcement. rb does not consume the emitter at runtime; ships the binary for parity with py/rs.",
        "MANIFEST UPDATE: sync-manifest.yaml::cli_variants.context/root.md.<codex|gemini>.headroom_floor_pct — per-CLI floor declarations (Risk-0004 baseline = 10).",
        "NEW RULE-EXTRACT: guides/rule-extracts/ci-runners.md — post-mortem evidence + full code examples for parent rule rules/ci-runners.md (exceeds 200-line budget).",
        "NEW RULE-EXTRACT: guides/rule-extracts/orphan-detection.md — post-mortem evidence + full code examples for parent rule rules/orphan-detection.md (exceeds 200-line budget).",
        "GLOBAL REFRESH: 48 globals updated (tier-filtered cc+co+coc subscription); 0 created; 609 unchanged.",
        "VARIANT OVERLAYS: 26/26 rb full-file overlays re-applied (13 rules + 8 commands + 5 skill SKILL.md). Pre-flight + post-apply verify-overlays.sh rb both 26/26 passing.",
        "SDK PINS: kailash-rs workspace 3.29.0 → 4.0.0 (read from kailash-rs Cargo.toml @ loom_sha cde61d3). Independent crates eatp/governance/pact unchanged at 0.1.0.",
        "STRUCTURAL ASSERTIONS (coc-sync.md Step 8): 17/17 hook scripts in settings.json resolve to .claude/hooks/*.js; zero scripts/hooks/ references in settings.json; obsoleted-path absence confirmed (scripts/hooks/, .claude/scripts/, scripts/resolve-template.js, terrene-naming.md, independence.md, etc.).",
        "PRESERVED: CLAUDE.md (template-owned, never overwritten per coc-sync.md Step 7); telemetry (.claude/learning/{observations,violations,learning-codified}.*) — never synced per sync-manifest.yaml::exclude.",
        "NOTE: rb is CC-only (clis: [claude], baseline_files: [CLAUDE.md]). Codex/Gemini emitter outputs (.codex/, .gemini/, .codex-mcp-guard/, AGENTS.md, GEMINI.md) intentionally absent per repos.rb.templates.[0].clis = [claude]. headroom_pct (codex/gemini) n/a for this template's verification table."
      ]
    },
    {
      "date": "2026-05-13",
      "version": "1.21.0",
      "summary": "loom 2.29.4 → 2.30.0 sync (rb CC-only): multi-CLI F-phase meta-skills shipped + classified. F1 (skill-authoring), F2 (command-authoring), F3 (hook-authoring) land canonical authoring references across CC + Codex + Gemini surfaces. Classification PR (#177) wires all three into sync-manifest.yaml::tiers.co, so rb (subscribed to [cc, co, coc]) ships them via co tier.",
      "changes": [
        "(prior changelog entries preserved — see 1.21.0 → 1.16.0 sections of prior VERSION file for full history)"
      ]
    }
  ]
}
