{
  "version": "1.22.0",
  "type": "coc-use-template",
  "description": "COC USE template for Ruby SDK projects (Magnus bindings) (regenerated by /sync from loom@cde61d3 + 2.31.0)",
  "released": "2026-05-15",
  "upstream": {
    "name": "loom",
    "type": "coc-source",
    "version": "2.31.0",
    "synced_at": "2026-05-15T00:00:00Z",
    "loom_sha": "f45759b",
    "template_version": "2.31.0",
    "sdk_packages": {
      "workspace": "4.0.0",
      "independent_crates": {
        "eatp": "0.1.0",
        "kailash-governance": "0.1.0",
        "kailash-pact": "0.1.0"
      }
    }
  },
  "changelog": [
    {
      "date": "2026-05-15",
      "version": "1.22.0",
      "summary": "loom 2.30.0 → 2.31.0 sync (rb CC-only): v6.2 per-lang headroom validator landing (Shards 1+2+3) + F5 Trust Posture Wiring on sync-completeness.md + 2 new global rule-extracts upstreamed from kailash-py. Shard 1 adds `validateAggregateHeadroom()` to emit.mjs with per-CLI `headroom_floor_pct` declared in sync-manifest.yaml::cli_variants.context/root.md.<cli>; Shard 2 wires `--strict-headroom` into coc-sync.md's baseline-emission step so /sync invocations propagate non-zero exit on breach; Shard 3 adds the `headroom_pct` column to sync-completeness.md Rule 2's verification table. F5 binds that structural defense to the Trust Posture system (regression-within-grace classes covered, two-tier near-breach receipt band, manifest-axis + exit-code-swallow sweeps at /codify). The 2 rule-extracts (ci-runners.md, orphan-detection.md) hold post-mortem evidence and full code examples that exceed the 200-line rule budget for their parent rules. CC-only template — Codex/Gemini emitter outputs not consumed; headroom_pct n/a for rb baseline emission. SDK PINS: kailash-rs workspace 3.29.0 → 4.0.0 (major bump). Pre-flight + post-apply verify-overlays.sh rb 26/26 passing.",
      "changes": [
        "RULE UPDATE: rules/sync-completeness.md — F5 Trust Posture Wiring landed (v6.2 headroom-floor BLOCK condition wired into trust-posture.md grace + regression classes). Both rule + rule-extract refreshed.",
        "BIN UPDATE: bin/emit.mjs — validateAggregateHeadroom() + `--strict-headroom` flag for per-lang aggregate headroom enforcement. rb does not consume the emitter at runtime; ships the binary for parity with py/rs.",
        "MANIFEST UPDATE: sync-manifest.yaml::cli_variants.context/root.md.<codex|gemini>.headroom_floor_pct — per-CLI floor declarations (Risk-0004 baseline = 10).",
        "NEW RULE-EXTRACT: guides/rule-extracts/ci-runners.md — post-mortem evidence + full code examples for parent rule rules/ci-runners.md (exceeds 200-line budget).",
        "NEW RULE-EXTRACT: guides/rule-extracts/orphan-detection.md — post-mortem evidence + full code examples for parent rule rules/orphan-detection.md (exceeds 200-line budget).",
        "GLOBAL REFRESH: 48 globals updated (tier-filtered cc+co+coc subscription); 0 created; 609 unchanged.",
        "VARIANT OVERLAYS: 26/26 rb full-file overlays re-applied (13 rules + 8 commands + 5 skill SKILL.md). Pre-flight + post-apply verify-overlays.sh rb both 26/26 passing.",
        "SDK PINS: kailash-rs workspace 3.29.0 → 4.0.0 (read from kailash-rs Cargo.toml @ loom_sha cde61d3). Independent crates eatp/governance/pact unchanged at 0.1.0.",
        "STRUCTURAL ASSERTIONS (coc-sync.md Step 8): 17/17 hook scripts in settings.json resolve to .claude/hooks/*.js; zero scripts/hooks/ references in settings.json; obsoleted-path absence confirmed (scripts/hooks/, .claude/scripts/, scripts/resolve-template.js, terrene-naming.md, independence.md, etc.).",
        "PRESERVED: CLAUDE.md (template-owned, never overwritten per coc-sync.md Step 7); telemetry (.claude/learning/{observations,violations,learning-codified}.*) — never synced per sync-manifest.yaml::exclude.",
        "NOTE: rb is CC-only (clis: [claude], baseline_files: [CLAUDE.md]). Codex/Gemini emitter outputs (.codex/, .gemini/, .codex-mcp-guard/, AGENTS.md, GEMINI.md) intentionally absent per repos.rb.templates.[0].clis = [claude]. headroom_pct (codex/gemini) n/a for this template's verification table."
      ]
    },
    {
      "date": "2026-05-13",
      "version": "1.21.0",
      "summary": "loom 2.29.4 → 2.30.0 sync (rb CC-only): multi-CLI F-phase meta-skills shipped + classified. F1 (skill-authoring), F2 (command-authoring), F3 (hook-authoring) land canonical authoring references across CC + Codex + Gemini surfaces. Classification PR (#177) wires all three into sync-manifest.yaml::tiers.co, so rb (subscribed to [cc, co, coc]) ships them via co tier.",
      "changes": [
        "(prior changelog entries preserved — see 1.21.0 → 1.16.0 sections of prior VERSION file for full history)"
      ]
    }
  ]
}
