{
  "version": "1.21.0",
  "type": "coc-use-template",
  "description": "COC USE template for Ruby SDK projects (Magnus bindings) (regenerated by /sync from loom@f4048e2 + 2.30.0)",
  "released": "2026-05-13",
  "upstream": {
    "name": "loom",
    "type": "coc-source",
    "version": "2.30.0",
    "synced_at": "2026-05-13T00:00:00Z",
    "loom_sha": "f4048e2",
    "template_version": "2.30.0",
    "sdk_packages": {
      "workspace": "3.29.0",
      "independent_crates": {
        "eatp": "0.1.0",
        "kailash-governance": "0.1.0",
        "kailash-pact": "0.1.0"
      }
    }
  },
  "changelog": [
    {
      "date": "2026-05-13",
      "version": "1.21.0",
      "summary": "loom 2.29.4 → 2.30.0 sync (rb CC-only): multi-CLI F-phase meta-skills shipped + classified. F1 (skill-authoring), F2 (command-authoring), F3 (hook-authoring) land canonical authoring references across CC + Codex + Gemini surfaces. Classification PR (#177) wires all three into sync-manifest.yaml::tiers.co, so rb (subscribed to [cc, co, coc]) ships them via co tier. Each meta-skill body explicitly covers the three CLIs (hooks.json registration per runtime, slash-command emission target paths, frontmatter contract); cross-references to cc-tier rules remain functional for CC consumers. Also lands BUILD-internal-path strip helper (bin/lib/strip-build-internal.mjs + audit-fixtures/strip-build-internal/) and manifest-aware variant overlay helper (bin/lib/variant-overlay.mjs + audit-fixtures/variant-overlay/) per loom PRs #166 + #168. CC-only template — Codex/Gemini emitter outputs not consumed. SDK PINS: kailash-rs workspace 3.27.0 → 3.29.0 (independent crates eatp/governance/pact unchanged at 0.1.0). Pre-flight verify-overlays.sh rb 26/26 passing.",
      "changes": [
        "NEW SKILL (F1, loom PR #170): skills/skill-authoring/SKILL.md — frontmatter discipline, description ≤200 chars, progressive disclosure 80/20, variant overlays, tools/allowed-tools handling, skill-embedded rule pattern, audit checklist.",
        "NEW SKILL (F2, loom PR #173): skills/command-authoring/SKILL.md — command vs skill vs agent decision matrix, ≤150-line body cap, neutral phrasing for cross-CLI emission, native-primitive carve-outs (codex review, etc.), sync-manifest wiring, audit checklist.",
        "NEW SKILL (F3, loom PR #176): skills/hook-authoring/SKILL.md — hooks.json registration per CLI, COC_RUNTIME closed-enum + parseHook canonical payload, Codex Bash-only event surface + .codex-mcp-guard fallback bijection, validator-13 predicate shapes A/B/C, instructAndWait emit() six-field halt shape, severity grounding (no block from regex), timeout fallback, workspace meta-dir filter, audit checklist.",
        "BIN UPDATE: bin/emit.mjs + bin/emit-cli-artifacts.mjs — manifest-aware variant overlay resolution (loom PR #168) + BUILD-internal path strip (loom PR #166). rb does not consume the emitter at runtime; ships the binaries for parity.",
        "NEW BIN LIB: bin/lib/strip-build-internal.mjs — helper that strips BUILD-internal source path references (src/kailash/, packages/, crates/) from synced content per coc-sync.md Step 3a.",
        "NEW BIN LIB: bin/lib/variant-overlay.mjs — manifest-aware variant overlay resolver for emit.mjs.",
        "NEW AUDIT-FIXTURES (3 fixture pairs + runner): audit-fixtures/strip-build-internal/{fixture-01,02,03}.{md,expected} + run.mjs — paired with bin/lib/strip-build-internal.mjs per cc-artifacts.md Rule 9.",
        "NEW AUDIT-FIXTURES: audit-fixtures/variant-overlay/{README.md,run.mjs} — paired with bin/lib/variant-overlay.mjs per cc-artifacts.md Rule 9.",
        "SDK PINS: kailash-rs workspace 3.27.0 → 3.29.0 (read from kailash-rs Cargo.toml @ loom_sha f4048e2). Independent crates eatp/governance/pact unchanged at 0.1.0.",
        "PRE-FLIGHT VERIFY-OVERLAYS: bash tools/verify-overlays.sh rb → 26 checks, 0 failing.",
        "STRUCTURAL ASSERTIONS (coc-sync.md Step 8): 17/17 hook scripts in settings.json resolve to .claude/hooks/*.js; zero scripts/hooks/ references in settings.json; zero obsoleted-path orphans (all 22 obsoleted/use_obsoleted paths confirmed absent).",
        "PRESERVED: CLAUDE.md (template-owned, never overwritten per coc-sync.md Step 7).",
        "PRESERVED VARIANT OVERLAYS: 13 rb rule overlays + 8 rb command overlays + 5 rb skill SKILL.md overlays (verify-overlays.sh 26/26).",
        "PRESERVED: telemetry (.claude/learning/{observations,violations,learning-codified}.*) — never synced per sync-manifest.yaml::exclude.",
        "NOTE: F1/F2/F3 ship as-is from globals — no rb variant overlays exist for skill-authoring/command-authoring/hook-authoring (multi-CLI by body design, no language-specific divergence at this stage).",
        "NOTE: rb is CC-only (clis: [claude], baseline_files: [CLAUDE.md]). Codex/Gemini emitter outputs (.codex/, .gemini/, .codex-mcp-guard/, AGENTS.md, GEMINI.md) are intentionally absent per repos.rb.templates.[0].clis = [claude]."
      ]
    },
    {
      "date": "2026-05-12",
      "version": "1.20.0",
      "summary": "loom@b50f869 → 1c3b964 catch-up sync (rb CC-only): value-prioritization MUST-6 + extract pairing (loom 1622108), coc-telemetry-autocommit hook + audit-fixture, settings.json hook wire-up, multi-cli-migration skill + /migrate command (caught prior-sync miss from b50f869 era), missing audit-fixtures for 5 violation-patterns detectors + detectRepoScopeDriftBash/upstream-remote subtree, skills/project/SKILL.md frontmatter. CC-only template — Codex/Gemini emitter outputs not consumed. SDK PINS unchanged (kailash-rs workspace 3.27.0). Pre-flight verify-overlays.sh rb 26/26 passing.",
      "changes": [
        "RULE UPDATE: rules/value-prioritization.md — MUST-6 added (Workspace-Survey + Verbatim Citation When User-Anchored Source Is Materialized); F-3.1 empirical results lifted (loom commit 1622108 from 2026-05-11).",
        "(prior changelog entries preserved — see 1.20.0/1.19.0/1.18.0/1.17.0/1.16.0 sections of prior VERSION file for full history)"
      ]
    }
  ]
}
